# tw_stock_db客製: 2026-09-17使用者要求——floating-assistant.js（AI引擎本體）
# 改成以desktop-app分支的renderer/floating-assistant.js為唯一canonical
# 來源（直接在那邊編輯/commit/push），main分支不再持有這個檔案的實體副本
# ——main分支的web/index.html改成執行時從desktop-app分支的raw URL fetch
# 這個檔案內容再執行（見index.html裡既有sql.js的fetch+Blob載入模式，
# 這次套用同一招）。理由：這個檔案改動頻率非常高，如果main持續追蹤它，
# 每次AI引擎的修改都會在main分支留下一筆跟「網頁本體」無關的commit，
# 把main的commit log弄得很亂，不容易看出網頁本體實際的變更歷史。
#
# 本機開發仍然可以保留這個檔案（方便離線快速測試，不用每次都真的連網
# fetch），只是不會被git追蹤、不會進main分支的commit——本機檔案要更新到
# 桌面版最新版本時，自己手動跑
# `git -C ../videos-嗨投資-desktop-app/desktop-app show desktop-app:renderer/floating-assistant.js > web/floating-assistant.js`
# 同步一份即可。
web/floating-assistant.js

# tw_stock_db客製: 2026-09-17使用者要求——daily/daily-chip/reports/watchlist
# 這四類股市更新資料改成各自獨立的force-push分支（理由跟上面floating-
# assistant.js完全一樣：這幾類資料每天/每次執行都會自動commit好幾次，把
# main分支的commit log淹沒，讓「網頁本體實際改了什麼」很難從commit歷史看
# 出來），main分支不再追蹤這四個目錄；2026-09-17後續使用者進一步要求連
# 過去的歷史commit都要清除，已經用git filter-repo重寫main整個分支的歷史
# +force push，這四個目錄現在完全不存在於main的任何commit（包含歷史）。
#
# 本地端的run_daily_update.py/sync_to_github.py仍然會繼續把資料寫進這四個
# 目錄（當作推送到新分支前的本地暫存區，也給其他本地流程讀取），只是不再
# `git add`/commit這些內容——見sync/push_branch_snapshot.py。網頁版改成從
# raw.githubusercontent.com/sunneo/tw_stock_db/{daily,daily-chip,watchlist,
# reports}分支直接抓取（見web/index.html的RAW/DAILY_MANIFEST_URL等常數），
# 不再依賴main分支底下的這幾個資料夾。
/daily/
/daily-chip/
/reports/
/watchlist/
