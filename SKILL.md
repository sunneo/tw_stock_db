---
name: stock-pattern-analysis
description: 台股型態學與技術分析選股教學能力，整合朱家紅老師與林穎老師的教學方法論（型態辨識、進出場紀律、資金管理、大盤連動、目標價估算）。當使用者要求選股、技術分析、型態判讀、持股診斷、盤後分析、週K走圖分析、或提到「朱老師」「林穎老師」「型態學」「頭肩底」「W底」「四線多排」等台股技術分析術語時，務必使用此技能。也適用於建置台股資料庫、爬蟲、每日技術指標計算、與圖文簡報報表產出等相關工程任務時，作為分析邏輯的依據。
---

# 台股型態學技術分析教學技能

本技能整理自「朱家紅老師型態直播班」與「林穎老師型態圖形班」課程內容的方法論摘要（來源：使用者的 NotebookLM 筆記本，已用自己的話重新整理，非逐字引用）。用途是讓 Claude 在做選股建議、型態判讀、持股診斷、盤後分析、週走圖報表時，能貼近兩位老師一貫的分析邏輯與紀律風格。

**重要提醒**：這是教學方法論的重整，僅供分析框架參考，不構成投資建議。任何實際操作建議都應提醒使用者這只是技術面框架，不是保證獲利的公式。

## 核心哲學（兩位老師共通點）

1. **紀律優先於預測**：型態學是機率統計工具，不是絕對預言。進場前要有完整計畫，進場後嚴格執行 SOP，不摻雜個人情感或消息面。
2. **大盤決定積極度**：個股永遠在大盤的脈絡裡操作。大盤多頭時可提高資金部位、抱波段；大盤走弱或空頭時，只能做短線、資金部位大幅降低，跌破關鍵均線就出場。
3. **位置決定策略**：股價相對於均線（尤其月線）的位置，決定該用什麼策略應對，而不是看基本面好壞。
4. **股票沒有好壞，只有多空**：不對特定標的產生感情，只有「做多 / 做空 / 不做」三種選擇。

## 背景知識參考檔案（核心原則 + 動態讀取）

以下每個主題都在這裡留下**核心原則摘要**，讓 Claude 不需要每次對話都全文讀取所有 `references/` 檔案就能掌握重點；只有當使用者的問題需要更細的規則、公式、數字門檻、或具體案例時，才動態讀取對應檔案的完整內容（用 `view` 讀取 `references/{檔名}.md`）。

### `references/pattern_recognition.md` — 型態辨識
- 底部反轉型態：頭肩底（頸線＝左右肩反彈高點連線，右肩不可跌破中心低點到反彈高點一半，需帶量突破頸線）、W底（建議等回測頸線不破再進場，勝率較高）、N字底（第二次探底高於第一次）、圓弧底、下降楔形、三重底/箱型整理、喇叭型擴張底。
- 頭部型態與底部型態邏輯鏡像對稱（M頭、頭肩頂）。
- 中繼型態（旗形/三角形收斂）成功率通常高於反轉型態；理想突破位置約在收斂區間長度75%–80%處。
- 型態驗證鐵律：突破頸線/壓力線當天須實體紅K＋放量，否則視為假突破；「關前放大量卻未突破」代表賣壓沉重。
- 四線多排（5/10/20/60日均線由上而下排列且上揚）代表多頭趨勢強勁，型態在月線正上揚處突破成功率更高。
- **需要細節時讀取全文**：判斷某檔股票具體是哪種型態、要看對稱性/量能細節時。

### `references/entry_exit_rules.md` — 進出場規則與資金管理
- 朱家紅老師三段式進場：試單點（型態近完成）→確認點（突破頸線）→加碼點（回測不破）。
- 林穎老師二分法：多頭「回後買上漲」（回檔不破前低＋紅K站回5日均線）／空頭「彈後空下跌」（反彈不破前高＋黑K跌破5日均線）。
- 停損三法可搭配使用：K線停損（守進場當根低點或前轉折低點）、百分比停損（5%–7%）、趨勢停損（頭部走低絕對出場，不論盈虧）。
- 目標價估算：等幅測量法（頸線到低點價差往上加計）、N字測量法（AB=CD，前一波漲幅往上加計）。
- 資金部位隨大盤動態調整（多頭7–8成／盤整轉弱降到4成以下），不攤平（除非歷史低檔分批布局，最多三次）。
- 三指標背離（價/量/KD/MACD不同步）是反轉先期訊號。
- **需要細節時讀取全文**：要給出具體停損價位、目標價計算、或資金配置建議時。

### `references/terminology.md` — 常用術語定義
- 快速對照表：回後買上漲、彈後空下跌、變盤線、四線多排/空排、凹洞量、關前放大量、確認整理/確認盤整、試單點/確認點/加碼點、三指標背離。
- 本檔案內容本身就很精簡（術語定義為主），**遇到使用者提到任一術語但語境模糊時，直接讀全文確認精確定義**，避免望文生義。

### `references/trading_discipline_series.md` — 交易守則01-36 ＋ 趨勢轉折研究
- 切線角度是趨勢強度量尺；切線壓力/支撐作用遞減（第一次觸碰最易受阻，第二次約打對折，第三次以後易被穿越）。
- 飆股操作需要「技術判斷力＋敢不敢狠不狠的執行力」；三種加碼時機＝回後買上漲／突破長黑高點／盤整突破；前提通常是2-3個月以上底部整理＋帶量突破頸線。
- 均線功能三部曲（趨勢線/支撐線/壓力線）；魚身理論：魚頭（突破均線）進場，魚身（沿均線上漲）續抱，魚尾（跌破均線回檔）不追。
- 停利依操作週期不同（當沖2-3%／短線5-8%／中期20-30%／長期40-80%），停利是反人性行為，應在進場前就規劃好。
- 資金比例隨大盤走勢動態調整；長線機會看週線型態＋吸貨量＋大盤/類股/個股三方同步轉強。
- 基本面/消息面須用股價走勢驗證（利多不漲反跌＝藉利多出貨；利空不跌反漲＝藉利空洗盤），不可違反技術面紀律。
- **需要細節時讀取全文**：需要飆股操作SOP細節、均線交易法的具體條件組合、或資金管理數字時。

### `references/stock_selection_series.md` — 選股守則01-20
- 看圖選股七步驟：趨勢確認（頭頭高底底高）→均線多排→站上月線→判斷所處位置→評估壓力支撐→評估勝率與獲利率→綜合決策。
- 葛蘭碧八大法則：做多/做空各4個訊號，均線由下降轉平緩＋股價突破＝反轉訊號；乖離過大反彈/回檔向均線靠近為逆勢訊號（風險較高）。
- 六大指標速查：KD（黃金/死亡交叉需搭配趨勢判讀，80以上/20以下易鈍化，背離判斷反轉）、MACD（0軸上下決定多空格局，柱狀體背離）、RSI（逆勢指標，>80超買<20超賣）、乖離率/MA通道（正乖離過大有回檔壓力）、ADL（與大盤同步/背離判斷趨勢延續或反轉）、布林通道（8個買賣訊號，三軌收斂後放量突破會重新張口）。
- 支撐壓力角色會互換：轉折高點突破後轉為支撐、未突破前是壓力（反之亦然）；密集盤整區時間越長量越大，支撐/壓力越強。
- **需要細節時讀取全文**：要具體套用某個指標的公式、判斷鈍化/背離細節、或支撐壓力位置估算時。

### `references/volume_price_relationships.md` — 量價關係九宮格
- 核心框架：價（漲/跌/平）× 量（增/縮/平）＝九種組合，但同一組合在「底部」和「高檔」代表意義往往相反，須疊加「目前位置」判讀。
- 量能分級與定義：基本量（5日均量）、攻擊量（基本量1.2-1.3倍以上）、止跌量、進貨量、換手量（成功續漲）／出貨量（換手失敗或高檔開高走低收黑）。
- 判斷換手成功的關鍵價位：大量K線最高點（突破＝換手成功）、1/2價（未破＝洗盤）、最低點（跌破＝換手失敗轉出貨量）。
- **需要細節時讀取全文**：要判斷某天K線的價量組合在特定位置代表什麼意義時（九種組合逐一列有位置對照表）。

### `references/gap_theory.md` — 缺口理論
- 核心觀念：缺口力量大於K線力量；缺口位置（打底完成/上漲行進中/末升段竭盡等）決定意義，同樣是向上缺口意義可能天差地遠。
- 向上6種情境／向下5種情境互為鏡像；底部打底完成的突破缺口需大量配合，末升段竭盡缺口則是爆量後隨即量縮價漲乏力。
- 缺口形成四個關鍵支撐/壓力價位（由強至弱：上高價>上沿價>下沿價>下底價，向下對稱）。
- 真封口（大量實體K線收盤真正回補，支撐/壓力特性消失）vs 假封口（長影線/量縮/量能不符，行情易延續原方向）；三天內回補常視為假突破/假跌破。
- **需要細節時讀取全文**：要判斷某個缺口屬於哪種情境、或缺口支撐壓力價位的具體估算時。

## 資料庫來源（重要）

台股日K/技術指標資料庫由使用者在本機執行 `tw-stock-db` 爬蟲產生，並自動同步到 GitHub repository：

```
https://github.com/sunneo/tw_stock_db
```

該 repo 自己也有一份 SKILL.md（`main` 分支根目錄）詳細說明資料結構，此處摘要對齊後的重點：

### 分支結構

- **`main` 分支 — 逐日增量資料（每個交易日更新一次）**
  路徑：`daily/{日期}/`（例如 `daily/2026-07-24/`），每個資料夾內有 4 個 CSV：
  `stocks.csv`（當天股票基本清單，全量）、`daily_prices.csv`（當天全部個股OHLCV）、
  `technical_indicators.csv`（當天全部個股技術指標）、`market_index.csv`（大盤/櫃買指數）。
  每個 CSV 可直接用 `raw.githubusercontent.com/sunneo/tw_stock_db/main/daily/{日期}/{檔名}.csv`
  抓取，欄位與 `tw-stock-db` 的 schema 完全對應。**只需要最近幾天資料時，優先用這個分支**，
  不用整包 clone。

- **`db-snapshot` 分支 — 完整資料庫快照（每月覆蓋一次，不留歷史）**
  內容為 `tw_stock.db` 切成 **5 個 64MB 以內的 part 檔**（`tw_stock.db.part000` ~ `part004`），
  **不使用 Git LFS**（早期版本曾用 LFS，因免費頻寬額度僅 1GB/月太容易超額，已改為一般
  git 檔案 + 切割方式），另附 `merge_db.py`（純 Python 標準函式庫）與
  `tw_stock.db.manifest.json`（記錄 sha256 checksum）。

  取得完整資料庫的正確流程：
  ```bash
  git clone --branch db-snapshot --single-branch --depth 1 https://github.com/sunneo/tw_stock_db.git
  cd tw_stock_db
  python merge_db.py     # 合併 part 檔並自動核對 sha256 checksum
  ```
  合併後即為完整 `tw_stock.db`（約2年歷史，含近2年 daily_prices / technical_indicators）。
  **此流程已實際測試成功**（clone + merge + checksum 驗證通過，資料庫可正常查詢）。
  這個分支**只在需要完整歷史（例如長天期均線、回測）時才 clone**，平常分析用 `main`
  分支的每日 CSV 即可，避免不必要的頻寬與時間成本。

### 使用建議（依任務類型選擇資料來源）

- 選股 / 鎖股名單 / 最近幾天的持股追蹤 → 讀 `main` 分支 `daily/{最近幾天日期}/*.csv`
- 需要完整歷史（回測、長天期均線、季線判斷等） → clone `db-snapshot` 分支 + `merge_db.py`
- 每次分析前先確認資料的最新交易日期，避免使用過期快照
- 若 GitHub 當下抓不到、或找不到對應日期的資料，才退回請使用者手動上傳 `tw_stock.db`

### 分析節奏：每日 vs 每週（重要）

GitHub repo 的 `main` 分支每個交易日收盤後會新增一份 `daily/{日期}/`，資料庫是持續累積的，
因此分析工作預設分成兩種節奏，依使用者的請求判斷該用哪一種：

**每日節奏（當日收盤後的輕量複查）**
- 觸發時機：使用者說「更新選股」「今天結果」「複查鎖股名單」等當日性用語。
- 流程：
  1. 檢查 `daily/{今天日期}/` 是否已存在（GitHub 更新通常在台股收盤 13:30 之後才會出現，
     盤中查詢請改用 claude-in-chrome 抓 `tw.stock.yahoo.com` 即時報價，不要空等 daily 更新）。
  2. 只拉當天的 4 個 CSV，upsert 進本地資料庫（不需要整包重下載或重 clone `db-snapshot`）。
  3. 直接執行 `python analysis/generate_holdings_report.py <鎖股清單.json> --compare-new`
     （見下方「常用工作流程」），重新計算前一批鎖股/強勢股名單中每一檔的最新分數與漲跌，
     自動產出「存活／跌出名單／新進榜」三分類比較，而不是每次都从零重新篩選整個市場、
     也不要手動下SQL逐檔重算（腳本已經把這套邏輯固定下來，跑一次幾秒鐘）。
  4. 若某檔觸及停損/停利門檻（entry_exit_rules.md 的規則，腳本已內建百分比/趨勢/K線
     三種停損判斷），報告會直接標註「出場」建議，需明確點名提醒。
- 輸出形式：文字摘要即可，除非使用者要求圖表/檔案。

**每週節奏（完整重新篩選＋報表）**
- 觸發時機：使用者說「這週的鎖股名單」「幫我做週報」「重新選股」等週期性/全市場用語，
  或該週尚未產生過完整篩選結果時。
- 流程：
  1. 直接執行 `analysis/generate_watchlist.py` / `analysis/screen_strong_stocks.py`
     （見下方「常用工作流程」），對全市場（約1985檔）套用固定好的篩選邏輯（四線多排/空排、
     站上/跌破月線、回後買上漲/彈後空下跌等，對應 `references/entry_exit_rules.md` ＋
     `references/stock_selection_series.md`），不要重新手動下SQL逐檔分析——腳本邏輯已經
     跟這兩份 reference 對齊，手動重新分析容易產生跟腳本不一致的結果。
  2. 產出完整的做多/做空鎖股名單、強勢股排行等（腳本已支援 json/csv/md 輸出），
     依使用者需求再轉成表格圖片、PPTX、PDF。
  3. 同步更新大盤環境判讀（多空排列、資金部位建議）。
- 輸出形式：使用者通常會需要正式報表（圖片/PPTX/PDF/CSV/JSON），依 SKILL.md 使用情境 C 的
  流程處理。

**共通原則**：不管哪種節奏，都先確認資料庫最新交易日期，並在回覆中註明資料截至日期，
避免使用者誤以為是即時盤中資料（除非確實有透過瀏覽器抓即時報價）。



- 資料源為 Yahoo Finance，法人買賣超/融資融券等籌碼面資料尚未納入。
- `db-snapshot` 分支每月被 force push 覆蓋，不要依賴其 commit 歷史，只信任最新一次 commit。
- 少數股票（例如 6174、8111）因公司名稱含 Big5/CP950 無法解碼的罕見字，`stock_name`
  會顯示亂碼字元，屬 TWSE 資料源本身限制，非本系統錯誤。

## 常用工作流程（優先用腳本產生，不要重新手動分析）

`tw-stock-db` 專案（本repo `code/tw-stock-db/`）已經把最常用的五種輸出固定成
`analysis/` 下的程式腳本。**收到以下需求時，優先在 `code/tw-stock-db` 目錄下執行對應
指令產生結果，讀取輸出檔案後再回覆使用者，不要重新手動下 SQL、逐檔分析全市場**——
那樣既慢又浪費大量 token，而且每次人工分析的判斷邏輯可能有微妙落差，跟腳本結果對不上。

執行前提（「本週＋完整資料庫＋當日資料」）：先確認資料庫是最新狀態——本機
`tw_stock.db` 已經跑過當天的 `run_daily_update.py`，或至少 GitHub `main` 分支
`daily/{今天日期}/` 已同步；資料不是最新時，先執行 `python run_daily_update.py`
（會自動更新股票清單/大盤/個股OHLCV/技術指標含RS值/每日報告），再跑下面的腳本。

| 使用者需求 | 指令 | 說明 |
|---|---|---|
| 鎖股名單 | `python analysis/generate_watchlist.py --method current --out 鎖股名單.json` | 目前原則(型態訊號評分)，做多+做空雙向 |
| 持股分析 | `python analysis/generate_holdings_report.py 鎖股名單.json --out report.md --compare-new` | 讀取既有鎖股清單，算P/L、停損停利、續抱/減碼/出場建議 |
| 強勢股（目前原則） | `python analysis/screen_strong_stocks.py --method current --top 30` | 型態訊號評分排行，5組訊號（KD交叉/MACD多空/紅綠柱由縮轉長/放量K/回後買上漲彈後空下跌）比對到幾個就是分數 |
| 強勢股（XQ原則） | `python analysis/screen_strong_stocks.py --method xq --top 30` | RS值(相對強度)動能排行，見下方「XQ原則的具體定義」 |
| 下週鎖股名單（XQ原則） | `python analysis/generate_watchlist.py --method xq --out 下週鎖股名單_XQ.json` | RS值動能篩選，只做多（RS值本質是動能排行榜，不適合套同一套邏輯挑放空標的） |

也可以一次跑齊前四種（週報）：`python run_daily_update.py --weekly`，輸出到
`reports/weekly/{今天日期}/`；想順便更新持股分析可加 `--holdings 鎖股名單.json`，
輸出到 `reports/holdings/{今天日期}.md`。所有腳本都接受 `--top`/門檻類參數調整，
細節見各檔案開頭的 docstring 或 `analysis/signals.py`（兩套評分邏輯的共用實作）。

### XQ原則的具體定義（RS值/相對強度）

使用者要求「參考XQ全球贏家的強勢股原則」時，依據的方法論來源是
[sysjust-xq/XQStrategy](https://github.com/sysjust-xq/XQStrategy)。**重要澄清**：
這個 repo 實際上是 XQ「選股中心」的通用選股條件腳本庫（依成交量/價格/量能/籌碼/
基本面/財務/事件分類、上百個 `.xs` 條件檔案，用 XQ 自己的 XS 語言寫成，
例如「收盤價距離N期高點在X%以內」「近N期至少M期成交量都大於X」這類可自由組合的
building blocks），**沒有單一「強勢股」標準公式**可以直接照搬。因此 `tw-stock-db`
採用業界最常見、也最貼近「RS值」精神的具體實作：

- **RS值**（`technical_indicators.rs_rating`，1-99分）：仿 XQ全球贏家／IBD（Investor's
  Business Daily）的「RS Rating」精神，以個股近3/6/9/12個月漲幅，用 40/20/20/20
  加權平均出原始動能分數，再對「當天全市場」做百分位排名（99=全市場最強）。由
  `compute_indicators.py` 的 `update_rs_ratings()` 計算，每次跑 `run_daily_update.py`
  （或手動跑 `compute_indicators.py`）都會更新最新交易日的排名。
- **XQ原則強勢股篩選條件**（`screen_strong_stocks.py --method xq` /
  `generate_watchlist.py --method xq`，門檻皆可用 CLI 參數調整）：
  RS值 >= 90（預設，約當前全市場最強的10%）、站穩季線（收盤價>=MA60 且 MA20>=MA60，
  代表中期趨勢向上）、量能未明顯萎縮（今日成交量>=5日均量*0.8）、價格貼近近60個交易日
  高點（15%以內，代表籌碼安定、隨時可能創新高，而非已經噴出一大段的末端）。

如果之後你在 XQStrategy repo 或其他管道找到更貼近原意的具體門檻/公式，直接調整
`code/tw-stock-db/analysis/signals.py` 裡 `xq_strong_stock_filter()` 的參數或邏輯即可，
不需要動其他腳本（`screen_strong_stocks.py` / `generate_watchlist.py` 都是呼叫這支
共用函式，改一處全部生效）。

## 使用情境與工作流程

### 情境 A：型態判讀 / 選股邏輯說明
當使用者問「這是什麼型態」「這檔股票可以買嗎」等問題時：
1. 先確認目前股價位置（月線之上/之下、月線走勢方向）
2. 判斷所屬趨勢（頭頭高底底高＝多頭；頭頭低底底低＝空頭；盤整）
3. 對照 `references/pattern_recognition.md` 找出型態特徵是否吻合，並檢查量價關係是否驗證型態（例如突破頸線是否有效放量）
4. 依 `references/entry_exit_rules.md` 給出進場條件、停損點、目標價估算方式
5. 一律提醒：這是技術面框架推演，非投資建議，且需搭配大盤環境判斷積極度

### 情境 B：持股診斷 / 持股追蹤分析
1. 抓取個股近期日K資料與均線位置（見資料庫 schema，若已建置）
2. 判斷目前處於哪個階段：型態進行中 / 已突破未回測 / 已達目標價 / 已跌破防守點
3. 給出「續抱／減碼／出場／加碼」的技術面判斷理由，並附上對應的量價證據
4. 同步檢視大盤位置，說明個股操作積極度是否該隨大盤調整

### 情境 C：盤後分析 / 週走圖報表
1. 大盤：判斷多空排列（四線多排/空排）、是否跌破月線或季線
2. 類股輪動：找出當日/當週表現最強與最弱的類股
3. 個股掃描：篩選符合「回後買上漲」「盤整突破」等進場條件的標的清單
4. 產出圖文並茂的網頁簡報（HTML + 圖表），版面與敘事風格盡量貼近兩位老師「型態＋SOP分開標註」的教學邏輯：先講型態辨識（機率統計），再講紀律執行（SOP）

## 風格備忘（MEMORY 摘要）

- 朱家紅老師：術語偏好「試單點／確認點／加碼點」三段式進場、「等幅測量」估算目標價、強調「會跑的才是贏家」的停損紀律。
- 林穎老師：術語偏好「回後買上漲／彈後空下跌」二分法描述多空進場點、常用「三指標背離」（價、量、KD/MACD）判斷反轉，停損分 K線停損／百分比停損／趨勢停損三種。
- 兩人皆强調「型態是機率不是保證」，進場後嚴格 SOP、不攤平（除非歷史低點分批布局）、不因基本面而違反技術面紀律。

## 已知限制

- NotebookLM 部分摘要來自語音直播的 AI 摘要整理，可能有轉譯誤差或遺漏；本機 docx 部分則來自投影片截圖的 OCR 辨識，個別數字或專有名詞仍可能有辨識誤差。遇到關鍵數字（例如成功率、百分比停損）時應向使用者確認原始課程內容。
- 使用者本機教材（`D:\Downloads\videos-嗨投資\教學`）已全數讀取整理完成：贏家交易守則01-36（全）、贏家選股守則01,03-20（19份，缺06）、量價01-04（全）、趨勢轉折全方位研究06、強勢上漲的訊號-4、決戰缺口專研班PDF（83頁全）。
- 「贏家選股守則06+個股討論.docx」因檔案上傳工具多次回報 Failed to upload file，尚未讀取，內容應與其他選股守則類似（技術面圖形＋個股案例討論），非核心方法論缺口。
- MP4 影片資料夾尚未處理（純影音檔案無法直接讀取文字內容，需要另外的語音轉錄流程）。
- 「20220401理周朱家泓老師每周精選多空鎖股追蹤.pdf」與「教學-清單.xlsx」分別為單週鎖股快照與檔案清單，非方法論內容，未收錄。
