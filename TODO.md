# xterm + Domain 架構 TODO（2026-09-18 起）

追蹤範圍：desktop-app 分支，floating-assistant.js 共用引擎 + bootstrap.js + main.js。
確認順序：**Phase 1（bug）→ Phase 2（xterm）→ Phase 3（domain 架構）**。
規則：個別完成、個別commit；未經明確「push it」不commit。

## Phase 1 — Bug 修復

- [x] Advance Settings 分組上線後，桌面版「桌面版設定」分頁（🔑API金鑰等）憑空消失 → 新增 `registerAdvancedSettingsTab()`，bootstrap.js 改用它
- [x] 桌面app不一定會堅持使用desktop op → 輕量stopgap：`delegate_to_subagent`額外補一個桌面版專屬的appendToolDescription補充（跟既有setEnvironmentNote是兩個獨立訊號通道）；過程中發現並修好一個真正的regression——`appendToolDescription()`補上去的文字原本會被`_updateDelegateToSubagentDescription()`的整段重建覆寫掉（`register_domain()`/`_saveAdvancedSettings()`都會觸發），現在`appendToolDescription()`會記住補充內容、重建時自動重新接上
- [x] 對於已安裝的Skill，AI呼叫得很薄弱/不穩定 → 找到真正根因：`delegate_to_subagent`自己的description從來沒提到使用者自訂的技能包/工具，根模型讀不到這件事就不會考慮委派，router完全沒有機會——已補上明確提示（不列出實際skill名稱，維持分層揭露原則）

## Phase 2 — xterm 終端機 widget（延續原 #3）

- [x] ~~cross-origin isolation阻塞~~ → 使用者提出更好的方向：不用wasi-sh的`spawn()`（需要crossOriginIsolated/SharedArrayBuffer），改用「指令攔截」——shell迴圈完全自己用JS實作，一般指令交給既有的`run()`（bash_execute同一個函式，純批次執行，完全不需要COI），vim/less/top這類全螢幕程式改成JS實作的bundle接管畫面。main.js的COOP/COEP標頭+SharedArrayBuffer feature flag+local-proxy.js的靜態檔案服務保留著當一般基礎設施（無害，將來可能用得到），但不再是這個功能的必要條件。
- [x] xterm.js載入（`FA_ASSET_URLS.xtermJs`/`xtermCss`，UMD build，跟其餘CDN函式庫同一種lazy-load模式）
- [x] Shell loop（JS實作的行編輯：印字/backspace/Enter/Ctrl+C/上下鍵翻歷史；`cd`/`pwd`/`clear`/`exit`/`help`這五個JS層攔截處理，維護跨指令的虛擬cwd狀態；其餘指令交給`run()`+`onOutput`即時串流輸出）
- [x] `/run-terminal` slash command：不加參數＝只開互動終端機；加參數＝開啟後直接執行；已在slash指令選單可見
- [x] ~~popup modal~~ → 使用者明確糾正：「不是要popup modal，是要嵌入對話的互動widget」。改成跟3D場景/音檔播放器同一種模式：`_handleRunTerminalCommand()`推一則帶`_displayTerminal`（非可枚舉，不會送進API payload）旗標的訊息，`_renderSingleMessage`偵測到時掛載xterm.js進訊息卡片，用既有的`this._liveWidgetCache`（WeakMap）讓widget在`_renderMessageHistory()`整個重繪時重用同一個節點、session狀態（scrollback/游標/歷史/虛擬cwd）不會歸零。每次`/run-terminal`呼叫都是對話裡獨立一則、可以同時存在多個session（已驗證）。原本的singleton `this._terminalSession`＋`_openTerminalModal`/`_closeTerminalModal`整套拿掉，改成`_mountTerminalWidget(container, initialCommand)`＋所有shell方法都改成顯式接收`session`參數，不再讀取單一全域欄位。`exit`不再是「關閉視窗」，只是標記`session.ended`、停止接受鍵盤輸入，畫面內容留著供之後回顧。
- [x] Terminal program bundle機制（`registerTerminalProgram()`/`_ensureTerminalProgramLoaded()`）：vim/less/top這類指令設計成可外掛抓回來的獨立bundle（呼應使用者原話），不寫死進floating-assistant.js。bundle檔案放在`desktop-app/renderer/terminal-programs/`，用相對路徑`import()`（跟CDN絕對網址分開處理，見`_ensureTerminalProgramLoaded`裡的判斷）
- [x] `less`/`more`（共用`terminal-programs/pager.mjs`）：翻頁瀏覽文字檔，space/f下頁、b上頁、j/k或↑↓單行、g/G頭尾、q離開。已驗證：分頁/邊界clamp/離開回到shell/檔案不存在的錯誤處理都測過
- [x] `vi`/`vim`（共用`terminal-programs/editor.mjs`）：極簡modal editor——normal mode（hjkl/方向鍵移動、`i`/`a`進insert、`x`刪字元、`dd`刪行、`0`/`$`行首/行尾、`:`進command mode）、insert mode（打字/Enter換行/Backspace，Esc返回normal）、command mode（`:w`存檔、`:wq`存檔離開、`:q`/`:q!`離開）。已驗證：新檔案建立、insert打字、存檔、reopen驗證內容、`x`刪字元、`:q!`正確放棄未存檔變更
- [x] 終端機可用性補強（2026-09-18使用者實測回報一輪）：`which <指令...>`（JS層攔截，答案來自`TERMINAL_SHELL_BUILTINS`/`TERMINAL_BUSYBOX_APPLETS`/已登記bundle清單，因為wasi-sh的busybox applet集合本身沒有`which`）；`/bin`、`/usr/bin`裡塞進同一份清單的空白佔位檔案（`_ensureTerminalFsStore`），`ls /bin`可以探索有什麼內建指令；`chmod <mode> <檔案>`（沙盒fs不追蹤真實權限，改成`session.execMarks`自己記一份哪些絕對路徑被授權執行，支援`+x`/`-x`/`u+x`類語法跟數字mode）；`sh`/`bash <script> [參數...]`＋`./script.sh`（含斜線路徑，需要先chmod +x）都能真的執行——因為busybox ash本身沒有fork/exec、「含斜線的指令名一律找不到」（wasi-sh README原話），這裡是JS層讀出檔案內容當一段新script文字餵給`run()`，繞過「沒有exec」的限制，shebang行會被ash當成一般comment處理，不影響執行；`time <指令...>`量牆鐘時間（沒有真正行程可量CPU時間，user/sys欄位固定印0，help文字有註明）。已在真實瀏覽器驗證：`ls /bin`列出全部內建指令、`which`四種分類都對、`chmod +x`前後`./script.sh`從權限不足變成可執行、`sh`/`bash`帶參數（`set --`）都正確傳進`$1`、`chmod -x`撤銷後恢復權限不足、shebang行/數字mode(755)都正確處理、`time`輸出格式正確
- [x] 終端機bug修復（2026-09-18使用者網頁版/桌面版交叉實測回報第二輪）：(1)`vim`在網頁版報「Failed to resolve module specifier」——根因是網頁版floating-assistant.js是fetch()+Blob URL執行的（見web/index.html），blob: URL沒有路徑階層可以解析相對import()，桌面版（同目錄真實檔案）沒這個問題所以先前沒測出來；修法：`_ensureTerminalProgramLoaded`先照舊試相對路徑，失敗才退到新增的`FA_ASSET_URLS.terminalProgramsBackupBase`（jsDelivr GitHub CDN絕對網址，已實測content-type正確、import()可以直接吃）。(2)終端機widget重新整理頁面後消失——`_displayTerminal`旗標比照`_displayDataUrl`/`_reasoningDisplay`等既有「非可枚舉屬性額外存一份」慣例，新增`terminalMap`存/讀進`_persistChatHistory`/`_loadPersistedChatHistory`，之前只顧到「不要送進LLM payload」卻沒注意到會連對話持久化也一起漏掉。(3)`/bin/ask-floating-ai-assistant`這種完整路徑被誤判成「使用者腳本，要chmod +x」——`/bin`、`/usr/bin`底下塞的只是`ls`探索用的空白佔位檔案，不是真的腳本內容；修法：`/bin/<name>`或`/usr/bin/<name>`只要`<name>`是已知內建/applet/bundle，直接當成打了`<name>`本身處理，完全略過chmod檢查。(4)左右鍵/Home/End都沒作用、也沒有Tab補全——原本`session.line`是只能append的字串，沒有游標位置概念；改成`session.cursorPos`+整行重繪（`_redrawTerminalLine`），支援左右鍵移動、Home/End（兩種常見escape sequence都接）、Delete鍵、游標中間插入/刪除；Tab補全支援指令名稱（第一個word）跟檔名（其餘word，讀`session.fsStore.readdirSync`），單一候選直接補完、多個候選列出來+補到最長共同前綴，比照bash習慣。全部已在真實瀏覽器驗證：`/bin/pwd`跟`/usr/bin/which`正確執行、Home鍵正確把游標移到行首插入文字、Delete鍵正確刪除游標右邊字元、Tab補全單一/多重候選/檔名都對、`_persistChatHistory`→新instance`_loadPersistedChatHistory`確認`_displayTerminal`正確還原、刻意把floating-assistant.js放到沒有terminal-programs/子目錄的環境下確認相對路徑失敗時真的會退到CDN網址並成功載入bundle
- [x] File Access Point掛載（`/mnt/<FAP別名>`，之前deferred的設計，這次使用者實測「ls /，仍沒看到mnt」後補上）：WASM沙盒backend採用者確認的「hydrate-and-flush」模式（呼應wasi-sh README自己給OPFS/真實資料夾這類非同步後端的建議整合方式），且使用者明確要求flush**不要**每個指令自動觸發、要有一個明確的`sync`指令自己控制時機（「做個sync指令來確實flush」）。`_ensureTerminalFsStore`只在session啟動時列出目前**已經granted**的FAP、在`/mnt`底下各塞一個空目錄佔位（讓`ls /mnt`馬上看得到有哪些可用，內容還沒讀），實際檔案內容lazy hydrate：第一次有指令（`_runTerminalShellLine`/`_terminalCd`的target/`_terminalRunScriptFile`的檔名跟腳本內容）提到`/mnt/<label>`時，才用`rec.handle`這個真正的`FileSystemDirectoryHandle`把整棵目錄樹讀進沙盒fs（`_hydrateFapMount`/`_hydrateReferencedFapMounts`）——故意不透過`fap_read_file`/`fap_write_file`（那兩個是給LLM文字互動用的既有工具，純文字＋8000字元截斷＋二進位格式直接拒絕），直接讀寫原始bytes，任何格式都能正確掛載。`sync [FAP別名]`（JS層攔截，跟chmod同一種理由——busybox裡不可能有這種能力）無條件把整個`/mnt/<label>`子樹目前的內容寫回真正的FAP（不做diff/dirty-tracking，邏輯簡單、不會因為沒有真正mtime可用而漏掉變更），留空＝flush這個session目前已經hydrate過的全部FAP。**已知限制**：只會新增/覆寫，不會把沙盒裡刪除的檔案同步刪除真正的FAP檔案（風險較高，刻意不做）；session中途才新增授權的FAP不會動態出現在`/mnt`（要重新開一個新的`/run-terminal`才會看到）。已在真實瀏覽器驗證（mock `FileSystemDirectoryHandle`）：`ls /mnt`列出granted的FAP、`cd`/`cat`進巢狀子目錄正確lazy hydrate、修改既有檔案+新增檔案後`sync <label>`正確把兩者都寫回真正的handle（用mock的實際內容驗證，不只看終端機輸出文字）、`sync`未帶參數flush全部已hydrate的、對還沒hydrate過的FAP執行`sync`正確報錯
- [x] `top`（`terminal-programs/top.mjs`，共用bundle機制）：不重現真正的process table（沒有process可列），改成只顯示這個終端機session自己的資訊（存活時間、cwd），每秒自動重繪，q離開。為此在`_runTerminalProgramBundle`的ctx契約新增`ctx.readKeyOrTimeout(ms)`（沒按鍵時逾時resolve(null)讓畫面能定時重繪，不像`readKey()`永遠卡到下一個按鍵）跟`ctx.sessionStartedAt`（session掛載時間戳）——這兩個是bundle ctx契約的擴充，不影響既有less/vi等只用`readKey()`的bundle。已驗證：畫面正確顯示遞增的uptime、q正確離開回到shell prompt、`which top`/`ls /bin`都能看到
- [x] 新Configure分頁：Advance Settings新增獨立的「終端機」群組（跟「多媒體」同一層級，因為/run-terminal是桌面/網頁共用的功能本身，不是AI相關子設定），底下「xterm 終端機」分頁涵蓋字體/字體大小/欄數(cols，0＝沿用xterm.js預設80)/可回捲行數(scrollback)/要不要scroll to output(關掉時新輸出不會打斷使用者正在往上捲看的位置)/佈景主題（system跟隨App目前亮暗模式，動態算，其餘ubuntu/powershell/黑底白字/白底黑字是TERMINAL_THEME_PRESETS固定色票）。設定存在`advancedSettings.terminal`（`_normalizeTerminalSettings()`正規化），套用在下一次`_mountTerminalWidget()`（已掛載的既有終端機不會即時變更外觀）。已驗證：改設定→存檔→掛載新終端機→確認xterm的fontFamily/fontSize/cols/scrollback/theme全部正確反映
- [x] 終端機資源管理：一次對話裡可能同時存在多個`/run-terminal`嵌入session，這裡控制哪些session維持正常背景重繪頻率（例如`top`每秒刷新）、哪些自動放慢（5秒）省資源——不影響手動輸入，只影響「沒有操作時畫面自己更新」的頻率。同一個「終端機」分頁裡設定政策：`focus`（預設，只有目前有鍵盤焦點的維持正常頻率，靠`term.textarea`的原生focus/blur事件判斷）／`viewport`（依`IntersectionObserver`判斷是否捲動到可視範圍內）／`count`（只有最近互動過的N個維持正常頻率，N＝`resourceCountLimit`，靠`session.lastActiveAt`排序）／`unlimited`（不節流）。`_getMountedTerminalSessions()`直接查DOM（`.ai-terminal-embed`元素上的`._terminalSession`）取得目前掛載中的session，不另外維護一份instance-level registry（避免訊息被壓縮/清除後還持有已經沒人看得到的session物件、阻止GC）。bundle透過新增的`ctx.idleRefreshMs()`拿到目前該等多久，`top.mjs`已改用它取代原本寫死的1000ms。已驗證：`count`政策正確識別最近互動的N個、`focus`政策正確依`hasFocus`判斷
- [x] 終端機外框加大＋PPTX/PDF匯出＋refresh恢復畫面（2026-09-18使用者實測回報第三輪）：(1)外框640x360加大到900x480（`max-width:95%`維持不變，小螢幕仍會自動夾住）。(2)終端機卡片新增📤匯出按鈕，重用既有的`_appendCardExportButton`（跟3D場景/繪圖卡片同一個機制），`getSnapshotFn`用lazy lookup讀取當下`._terminalSession`＋`_captureTerminalScreenText`；PPTX/PDF的視覺snapshot共用邏輯（`_faAppendVisualSnapshotSlides`/`_faMarkdownToPdfBlob`）新增`kind:'terminal'`（沿用既有`viewer_summary`的純文字render分支，只是換一個標籤「🖥️ 終端機記錄」）。(3)refresh恢復畫面：新增`_captureTerminalScreenText(session)`（讀`term.buffer.active`，比照使用者建議的「rotatelog」精神——xterm本身的scrollback設定已經是環狀緩衝區，這裡再疊加一個獨立的500行上限，避免使用者把scrollback設很大時持久化快照也跟著肥大）跟`_persistTerminalSnapshot(session)`（每個指令執行完就呼叫一次，寫回`msg._displayTerminal.savedScreen`＋觸發`_persistChatHistory()`——不是每個按鍵都存，太頻繁）；`_mountTerminalWidget()`多一個`msg`參數，重新整理頁面後重新掛載時如果`savedScreen`有值，先把它印出來當畫面回顧、加一行灰字說明「這是全新session不是恢復原本程式」，再照常開一個全新session等待輸入——刻意**不**嘗試恢復WASM shell的process狀態，使用者原話「不用保留process狀態，就讓終端機的command恢復等待輸入」。**實測過程中抓到一個真的bug**：xterm.js的`term.write()`是非同步佇列處理（不是同步寫進buffer），一開始捕捉savedScreen時漏掉了剛執行完的指令輸出/新prompt，改成寫一個空字串+callback（利用xterm內部FIFO佇列的順序保證）等前面所有排隊的write都真的處理完才讀buffer，`_persistTerminalSnapshot`跟匯出按鈕的`getSnapshotFn`都套用了同一個修法。已在真實瀏覽器驗證：外框尺寸正確、指令輸出後`savedScreen`正確包含完整輸出（含修好時序bug後的驗證）、模擬重新整理後正確回放先前畫面+接上全新可用的prompt、點擊匯出PPTX確實產生一個854KB的真實.pptx檔案（不是只檢查UI文字，是直接確認`fa.messages`裡多了一筆`_downloadFile`紀錄）
- [ ] 終端機後端切換設定：優先WASM模擬器 vs 使用者真實系統shell（網頁版只能選模擬器）——**目前只有WASM emulator這一條路徑，真實系統shell backend還沒做**；使用者已確認：真實shell backend走真正的OS pty，vim/top/less應該原生就能動，不需要這裡的intercept/bundle機制。2026-09-18使用者提出設計問題並確認方向：真實shell backend底下`ask-floating-ai-assistant`要怎麼變成一個PATH上真的能exec的指令？**已確認：trampoline/shim script方案**——session啟動時在tmpdir產生一個小的shim執行檔（POSIX shell script/Windows對應格式），塞進spawn真實shell時的PATH最前面；shim本身呼叫`local-proxy.js`既有的本機HTTP server（已經在跑，之前COI實驗加的靜態檔案服務可以順便掛同一個server）新增一個路由，把args/stdin轉成HTTP request送過去，main process收到後橋接進renderer（`webContents.send`+`ipcMain.handle`那種round trip）呼叫真正的`_runSubAgentTask()`（2026-09-18第三輪修正後的實際呼叫對象，見下面`ask-floating-ai-assistant`那行的說明——不是`_delegateToSubagentAuto()`），回應原封不動印回shim的stdout。不做「真的編譯/注入一個原生程式」——反正這個指令本質上一定需要某種IPC橋接回Electron主體的LLM邏輯，shim+HTTP這條路徑最簡單、跨平台、不需要簽章/編譯，且session結束可以直接砍掉tmpdir。**還沒開始實作**，等real shell backend本身開始做時一起做
- [x] `ask-floating-ai-assistant`：JS層攔截指令（`_terminalAskAI`），`[--format text|json] <問題>`語法。**設計經過三次使用者糾正定案**：一開始做成獨立單次LLM問答（`_runIsolatedLlmCompletion`，無重試），使用者回報「那個回應500是不被接受的」+「他的邏輯必須跟FloatingAssistantApp本體一樣/跟floating-assistant.js一樣」+「體驗上必須完全跟cli版本一致」（追問確認：cli模式其實跑的是完整agentic loop，會叫工具、多輪對話，「跟_runSubAgentTask同等級」）。第二版改成呼叫`_delegateToSubagentAuto()`（`delegate_to_subagent`工具domain留空時呼叫的同一個方法），但使用者接著實測回報「hi」「現在幾點」這類不需要專家領域的簡單問題一律被拒絕（「自動判斷後認為這個任務不需要用到任何專家領域…」）——根因：`_delegateToSubagentAuto()`是設計給根模型當「委派」判斷用的，判斷不需要委派時，正確反應是把結論交還根模型自己回答，但`ask-floating-ai-assistant`這裡沒有根模型可以接住這個拒絕、它自己就該是那個根層級。**第三版（目前版本）**：直接呼叫`_runSubAgentTask(prompt, SUBAGENT_DELEGATE_MAX_ROUNDS)`（不傳`allowedToolNames`/`systemPrompt`）——這個方法在`allowedToolNames`留空時本來就是「看得到全部工具+主system prompt」的根層級agentic行為（跟main.js `runCliPrompt()`的`fa._submitChatInput()`同一個層級），不需要domain路由這道閘門，「hi」這類問題會直接被回答。等待期間的心跳訊息刻意抄`main.js`的`runCliPrompt()`CLI心跳格式（每5秒一次、同樣文字）。`--format json`輸出格式（ok/prompt/response/elapsed_ms）比照`cli-format.js`的欄位精神。已驗證：stub `_runSubAgentTask`後確認一般問答（含「hi」這類簡單問句不再被拒絕）、`--format json`（確認旗標不會混進prompt文字）、心跳確實每5秒觸發一次且完成後正確清除（沒有殘留interval）。**WASM emulator已支援；真實shell backend還沒做（backend本身都還沒做）**
- [x] `python`/`python3`（Pyodide）、`jq`/`xq`/`column`/`split`在終端機裡不能用——根因：`bash_execute`工具的`run()`呼叫本來就有替這幾個指令按需下載執行環境、註冊成host builtins的機制（`SANDBOX_COMMAND_REGISTRY`），但終端機自己的`run()`呼叫點（`_runTerminalShellLine`/`_terminalRunScriptFile`）從一開始就沒有比照辦理，完全沒有傳`builtins`參數。新增`_buildTerminalSandboxBuiltins(script)`複製`bash_execute`完全同一套邏輯（word-boundary正則掃描script文字→只await真的用到的引擎→組成純同步builtins map），三個呼叫點共用；同時比照`bash_execute`維護`this._activeSandboxFsStack`（python的`subprocess.run(['sh','-c',...])`回頭呼叫shell時要沿用同一個store）。`/bin`、`/usr/bin`佔位檔案+`which`+Tab補全的候選清單都一併加上`SANDBOX_COMMAND_REGISTRY`這幾個名字。已在真實瀏覽器驗證：`python3 t.py`正確印出腳本輸出（含Pyodide首次下載+初始化的等待）

## Phase 3 — Domain / Subagent 架構

- [x] 新增「研究」subagent domain（`research`，`SUBAGENT_DOMAIN_REGISTRY`）：唯讀分析domain，刻意跟真的會寫入/執行的domain（`file_access_points`/`git_operations`/桌面版`desktop_ops`）分開，只給FAP唯讀工具+摘要工具，系統提示第一件事是判斷使用者要「修改/實作」還是「分析/理解」，前者要主動指出該改委派給有寫入能力的domain。桌面限定的`fs_read_file`/`fs_list_files`/`fs_find_file`/`fs_stat`（唯讀，不含寫入/執行）跟目前workspace目錄提示由`bootstrap.js`在`fa`建構完成後用「先讀既有toolNames、合併、整段`register_domain()`寫回去」的方式疊加，不是憑空覆寫掉web版的基礎定義。已驗證：base domain正確註冊、桌面擴充合併正確（沒有寫入/執行工具混進去）、dynamicNote正確回報workspace目錄
- [x] 目前subagent domain清單UI＋Domain管理UI＋AI revise＋AI Dry Run（這四項Phase 3的原本第2/3/4/5項，因為天然是同一個UI，合併一起做）：Advance Settings新增「Domain 管理」分頁（`ai`群組底下，`subagent`跟`limits`之間）。清單顯示`this.domains`裡每一個domain（排除`skill_<id>`這幾個自動產生的——那些已經有自己的「Skill」分頁管理，混進來會兩份UI打架），標「內建」/「自訂」badge（判斷依據：key有沒有出現在`advancedSettings.customDomains`裡）、啟用切換checkbox（直接在列表上快速切換，不用開完整編輯器）、修改按鈕（開editor modal：key/label/category/工具名稱清單(textarea一行一個)/描述(systemPrompt)/啟用）、刪除按鈕（只有自訂domain才顯示）。**內建domain一樣可以「修改」**（describe內容/工具清單都能改）——儲存時統一寫進`advancedSettings.customDomains`（不分內建/自訂，用同一個陣列＋`_syncCustomDomains()`真正呼叫`register_domain()`套用），這帳戶了「每個domain可以自己描述內容」這個要求；差別只在「刪除」：`_syncCustomDomains()`清掉上一輪自己註冊的key時，如果那個key本來就在`SUBAGENT_DOMAIN_REGISTRY`模組常數裡，還原成內建預設值（不是整個消失），真正全新的自訂key才會整個delete——**已知邊界情況**：host在執行期註冊的domain（例如桌面版`desktop_ops`，不在那個模組常數裡）如果被覆寫又刪除覆寫，目前只能整個delete、沒有原始值可還原，要等下次app重啟host重新`register_domain()`才會恢復（不是永久遺失，只是當前session內有這個小缺口，接受這個取捨換取其餘情況的正確性）。「AI revise」按鈕在編輯器描述欄位旁，重用`_callRouterLLM`（純文字分類請求，不開agentic loop）把這個domain的描述跟系統裡其他domain的代號+名稱一起丟給LLM，specifically要求檢查跟其他domain的字面關鍵字碰撞（呼應這次session前面`file_analysis`/`file_access_points`那次真實bug的教訓）。「AI Dry Run」在分頁下方，輸入一個假設任務，直接重用`_routeTaskToDomains`/`_routeTaskHierarchical`（跟真正委派時同一套路由邏輯，不是另外兜一份模擬邏輯，結果才有意義）取得會路由到哪些domain，不會真的執行`_runSubAgentTask`，另外用一個要求輸出`{"reasoning":...}`的補充LLM呼叫取得簡短理由。已在真實瀏覽器完整驗證：清單正確渲染（含內建/自訂badge）、新增全新自訂domain成功、覆寫內建domain（drawing）後刪除正確還原成原始描述（**過程中抓到一個真的bug**：`_syncCustomDomains()`一開始寫成無條件`delete`，完全沒有還原內建預設值這個分支，這個bug只有在瀏覽器裡實測「覆寫→刪除→檢查是否還原」這個順序才會現形，光看程式碼容易誤以為邏輯正確，修好後才確認真的能還原）、全新自訂domain刪除後確實完全消失、AI Dry Run正確回報路由結果+理由（stub `_callRouterLLM`測試）、AI revise正確回報檢查結果。**2026-09-18隨後使用者明確糾正**：「內建的domain不提供修改，只能檢視」——上面「內建domain一樣可以修改」那段設計被撤回。改成`_openDomainEditor()`依「這個key有沒有出現在`advancedSettings.customDomains`裡」判斷`viewOnly`：內建/host註冊domain一律唯讀（所有欄位disabled、Save按鈕隱藏、標題顯示「檢視 Domain（內建，不能修改）」、清單上的按鈕文字改成「檢視」），只有真正自訂的domain才會顯示「修改」並能實際存檔。因為built-in永遠不會再進入`customDomains`，`_syncCustomDomains()`原本「還原成內建預設值」那個分支變成死程式碼，一起移除，簡化回單純的delete。列表上的啟用/停用checkbox不受這次糾正影響，內建domain仍然可以直接切換啟用狀態（那不算「修改內容」）。已在真實瀏覽器重新驗證：內建domain開啟後所有欄位disabled、Save按鈕不存在；自訂domain開啟後一切正常可編輯存檔
- [x] Skill編輯畫面：choose domain（像MediaWiki的category一樣，可選現有的、也可以create new）——這裡的「domain」對應到既有的Skill Bundle選單（每個skill bundle本身就是一個`skill_<id>` domain）：既有的「所屬技能包」下拉選單加一個「➕ 建立新的技能包(Domain)...」特殊選項，選中時當場跳`prompt()`問名稱、呼叫既有的`_createSkillBundle()`建立並自動選中（MediaWiki風格「打新名稱直接建立」，不用先跳出編輯器去「Skill」分頁另外建立再回來選）。已驗證：選中特殊選項→輸入名稱→選單值正確變成真正的bundle id、對應的`skill_<id>` domain確實被建立
- [x] Configure > AI：「主domain偏好」選項——放在「Domain 管理」分頁最上方（一個下拉選單，候選值是目前存在的domain，留空＝沒有偏好）。同時影響兩件事：(1) 路由權重——`_getDomainPreferenceRoutingHint()`疊加進`_routeTaskToDomains`/`_routeTaskHierarchical`第一層的router prompt，任務同時符合多個領域、界線不明確時優先偏向這個領域；(2) 回應風格——`_getDomainPreferenceNote()`疊加進`_getGroundingContext()`（跟`setEnvironmentNote`/系統時鐘同一個「根對話跟每個委派出去的子agent都看得到」的共用入口），內容是「跟這個領域相關的任務優先寫檔案/實際操作，不要把程式碼/結果貼在對話文字裡當答案」，呼應使用者原話「不只影響路由權重，也影響回應風格...不要把code當聊天內容直接回覆」。已驗證：設定後選單正確持久化、note/hint都正確反映所選domain的label、`_getGroundingContext()`確實包含這段文字（代表根對話跟子agent都會看到）

## Phase 4 — 程式設計 Domain（`coding`，桌面版真實git patch流程，專為弱模型設計）

設計文件見plan（外部化狀態機＋Plan Template＋DESIGN-INDEX，弱模型不必記住進度）。

- [x] main.js/preload.js：`fa:git:applyPatch`／`fa:git:inspect`兩個IPC（重用`runShellCommand`/`buildFullCommandLine`，不經`execConfirmGate`；applyPatch內部一律先`git apply --check`乾跑，失敗完全不碰檔案並原樣帶回git stderr；inspect支援status/diff/log/restore）＋`window.desktopAPI.git`命名空間
- [x] bootstrap.js：`apply_git_patch`／`git_inspect`／`coding_task_state`三個工具（後者純renderer實作，用既有`rawfs.readFile/writeFile`維護`.floating-assistant/coding-task-state.json`＋`DESIGN-INDEX.md`＋`tests/`，action：get/init/add_todo/update_todo/complete_todo/record_test_result/set_phase，每個action都做schema驗證）
- [x] `coding` domain註冊（緊接`desktop_ops`）＋固定流程systemPrompt（讀狀態→設計(Plan Template)→環境→測試/語法檢查指令→baseline→逐TODO：讀最新檔案→patch→語法檢查→diff確認→測試→complete_todo→commit；每項最多重試3輪；steering用add_todo插入；禁止request_additional_tools申請fs_write_file；絕不push）＋`_domainGatedToolNames`
- [x] 修正`research`／`desktop_ops`／根層級環境提示：寫程式/修bug類任務改指向`coding`
- [ ] 驗證（拋棄式git repo）：全新任務／壞patch被dry-run擋下且檔案未動／中斷後恢復／steering插入／語法檢查失敗後restore／3輪上限／非git repo詢問

驗證紀錄（2026-09-20，用不開GUI視窗的方式：mock `electron`載入**真實**main.js取出`ipcMain.handle`註冊的handler，並用vm執行從bootstrap.js切出的**真實**工具程式碼，對拋棄式git repo＋真實磁碟操作）：
- `fa:git:applyPatch`：context對不上的壞patch → `ok:false, stage:"check"`、stderr是git原文（`patch does not apply`）、**檔案內容位元不變**；`check_only`只乾跑不動檔案；好patch套用成功（含模型漏掉結尾換行的情況，工具會補）；套用後真的跑測試由FAIL變PASS。**抓到一個真bug**：Windows下暫存patch路徑的反斜線被Git Bash吃掉（`can't open patch 'C:UserssunneoAppData...'`），改成傳正斜線給git。
- `fa:git:inspect`：diff內容正確；把檔案寫成0 byte（模擬弱模型誤殺）後`restore`成功還原到最後一次commit；目標資料夾不存在／不是git repo都回明確錯誤。
- `coding_task_state`：get(無狀態)→exists:false；init缺design/todos被拒；進行中不能被re-init覆蓋；update_todo不能直接設done；complete_todo缺設計摘要/位置/入口被拒，成功時DESIGN-INDEX.md格式正確append；record_test_result寫進tests/；壞phase被拒；狀態檔被寫壞時回明確錯誤（不靜默覆蓋）；5個平行add_todo經per-cwd鎖序列化後id 1..5不衝突。**抓到一個設計缺陷**：steering用`position:"now"`原本排在進行中項目前面，但`next_todo_id`仍指向進行中項目、插隊沒有真的優先；改成「now」暫停目前項目（退回pending並留註記）、`next_todo_id`改依清單順序；同時修正`"next"`在沒有in_progress時會插到隊首的錯誤。
- **尚未驗證**：真實Electron app裡由各家弱模型實際走完整流程的遵從度（這需要實際接模型跑一輪，只能靠使用者實測回報後再調systemPrompt）；路由器是否確實把「寫程式/修bug」導向`coding`（已在根層級提示與research/desktop_ops補了指引，但沒有實際跑一次路由驗證）。

### Phase 4 追加：網頁版也支援（2026-09-20使用者要求「不一定要桌面限定」，網頁版有FAP、git、bash/python emulator）

- [x] `coding` domain改成桌面/網頁共用：`SUBAGENT_DOMAIN_REGISTRY.coding`（網頁版）＋桌面版`register_domain("coding")`覆蓋（真的git/run_command）；system prompt由純函式`_faBuildCodingSystemPrompt({kind})`產生（domain的systemPrompt必須是靜態字串），兩邊共用同一套固定流程，只有讀檔/測試/commit用的工具不同。狀態機（`coding_task_state`）整個搬進引擎（`_codingStateAction`），桌面版只提供rawfs的io，網頁版提供FAP DirectoryHandle的io——同一份邏輯不重複維護。
- [x] 網頁版工具（引擎內，`registerOptional`，全部domain-gated）：`coding_read_file`（完整內容＋行號，不像`fap_read_file`固定截斷8000字元）、`apply_git_patch`（純JS unified diff解析/套用：**比真的git寬鬆兩點**——不信任hunk header行數、行號可偏差（依內容找最近的吻合位置），但內容逐字比對不模糊；剝```fence、容忍被吃掉開頭空白的空context行、保留CRLF；多檔案all-or-nothing；拒絕把檔案清空成0 bytes；套用前自動備份原內容）、`git_inspect`（status/diff/log/restore/init，isomorphic-git＋自寫LCS diff；restore優先用git HEAD，沒有git或檔案不在commit裡時退回自動備份）、`coding_run_check`（.py用Python ast、.js/.mjs用acorn、.json、.sh用`sh -n`）、`coding_run_tests`（把專案文字檔複製進瀏覽器bash/python沙盒後執行；**沒有node、沒有網路，JS/TS專案的測試跑不了，prompt明訂要誠實告知，不可假裝通過**）。commit沿用既有`git_commit`。
- [x] 驗證（三層）：(1)純函式單元測試（解析/套用/diff產生，含header行數錯、行號偏差、fence、CRLF、新增/刪除檔案、多hunk、垃圾行）；(2)mock `FileSystemDirectoryHandle`（真實磁碟）＋真的isomorphic-git跑引擎原始碼區塊：壞patch被擋且檔案不動、check_only、0-byte清空被擋、備份→restore、git init/status/diff/log/restore、大檔分段讀取、狀態機（steering、DESIGN-INDEX、tests/、平行呼叫序列化）；(3)**真實瀏覽器**：domain註冊且10個工具都存在、根層級看不到這些工具、Pyodide沙盒跑真實python測試（含`import`同專案模組、套件、改壞程式後測試真的失敗且看到新程式碼、修好後通過）、py/js/mjs/json/sh語法檢查（含錯誤位置）、以及透過**真正註冊的工具**走完整流程（真失敗baseline→壞patch被擋→好patch→語法檢查→diff→測試通過→狀態/DESIGN-INDEX/tests/→commit→status乾淨→清空後restore）。
- [x] **測試中抓到的既有bug（順手修掉，`bash_execute`也受惠）**：(1)`_walkCtxFsDir`把目錄的`stat().type`（實測是`'dir'`）誤判成檔案去讀（回null→`.subarray`炸掉）——只要`/work`底下有任何子資料夾，`python`指令就整個失敗；(2)`python <相對路徑>`在shell cwd不是`/work`時找不到腳本；(3)python builtin沒設cwd/`sys.path`/`__file__`，測試腳本`from calc import add`永遠`ModuleNotFoundError`，且同一個pyodide instance會沿用上一次從`/work`載入的模組快取，改過的原始碼被舊模組遮住、測試結果是假的——改成執行前設cwd＋腳本目錄進sys.path＋`__file__`並清掉`/work`來源的模組快取，執行後還原。
- [ ] **尚未驗證**：真實網頁版（`web/index.html`）從`desktop-app`分支fetch這份引擎後、由各家弱模型實際走完整流程的遵從度；真實`showDirectoryPicker`授權的FAP（測試用的是mock DirectoryHandle）；`acornJs`目前只走CDN，沒有比照其他函式庫做GitHub備份分支。

## 零星bug修復（2026-09-20）

- [x] 開著「顯示工具呼叫追蹤與思考過程」時，AI回應期間正在展開檢視的區塊會被收起來——根因：每一輪工具呼叫都會觸發`_renderMessageHistory()`整個清空chatBody重繪，`<details>`每次都是重新建立（預設收起）。新增`_bindDetailOpenState(detailEl, owner, kind)`，把展開狀態記在「訊息物件」的WeakMap上（訊息被丟棄自動回收），重繪建立`<details>`時還原並持續追蹤toggle；套用在工具呼叫追蹤（原生function call與`[CALL:]`兩種）、工具結果、思考過程、已封存對話五處。已在真實瀏覽器驗證：展開兩個區塊後連續重繪兩次仍維持展開、手動收起後重繪維持收起、追加新訊息不影響既有區塊狀態。**未涵蓋**：`_renderSupersededDraftCard`（草稿卡，內容是懶惰掛載，本來就會依展開才建立）。


### Steering 轉送給子agent（2026-09-20）
- 問題：steering只寫進根 this.messages，委派中的 _runSubAgentTask 用區域messages，長時間子任務完全收不到插話。
- 修正：_addSteeringMessage 另存 this._steeringLog；_runSubAgentTask 每輪開頭把新的steering以user訊息注入自己的messages（並在進度卡顯示）。根層仍保留原 [Steering]，子任務回來後根模型也看得到。
- 限制：子agent正在等待單次LLM回應或長工具時要等該輪結束才看得到；未在真實長任務上驗證，只做語法檢查。

## Phase 5：skills domain＋Chrome 瀏覽器控制（2026-09-20）

- [x] `skills` domain（建立Claude格式skill）：`skill_create`（驗證name/description、組frontmatter、scripts/references檔案、dry_run/overwrite/download/save_to）、`skill_list`、`skill_read`；建立後即為app內skillBundle（Skill分頁可管理）、可匯出.skill。真實瀏覽器驗證：非法name/含claude被擋、dry_run警告未被引用的檔案、重複名稱需overwrite、匯出zip內容正確。
- [x] Chrome擴充功能 `Floating AI Assitant(Chrome Extension)`（`web/browser-control-extension/`，MV3）：分頁群組、分頁、導覽、捲動、截圖（含整頁）、滑鼠（click/雙擊/右鍵/移動/滾輪/拖曳）、鍵盤（文字含中文/按鍵/組合鍵）、取頁面文字與可互動元素座標。**只能操作助理自己開的分頁**；網頁版需在擴充功能彈出視窗「允許目前網站」，桌面版需配對碼。用Edge 153載入擴充功能實測全部指令（網頁橋＋桌面輪詢兩條路徑）。
- [x] 引擎工具＋`browser_control` domain（14個browser_*工具）；截圖以`{type:'image'}`直接顯示給使用者，模型端靠get_page_text/get_elements（純文字模型也能操作）。
- [x] 設定 → 「瀏覽器控制」分頁：「在瀏覽器測試連線」（未安裝／未允許／成功三種狀態）、「[get extension]」下載zip並展開安裝教學；桌面版多一個配對碼區塊（複製／重新產生／連線狀態）。
- [x] 桌面版：`desktop-app/browser-control-server.js`（127.0.0.1:17923，X-FA-Token驗證，CORS只放行chrome-extension://）＋`fa:bc:*` IPC＋bootstrap.js `setBrowserControlTransport`。擴充功能檔案放在`web/browser-control-extension/`（網頁與桌面版共用；引擎依序試部署目錄、raw GitHub後備網址）。
- 限制：Chrome 137+正式版已停用`--load-extension`，自動化測試改用Edge；正式安裝走「載入未封裝項目」。截圖/滑鼠/鍵盤使用chrome.debugger，操作時會出現「正在偵錯此瀏覽器」提示列。未用真實LLM跑過整段agent流程。

### Phase 5 追加：AI Controlled 群組／自動補分頁／自動開Chrome／全domain注入／結構化讀頁（2026-09-20）
- [x] 所有助理開的分頁一律放進單一「AI Controlled」分頁群組（已存在就加入、不存在就建；tab_group_create 的 title/color 不再有作用）。實測：兩次 tab_group_create＋tab_create 後 groups 只有一個。
- [x] 找不到分頁（tab_id 過期／被關／沒給）一律無條件在群組開新分頁，回傳 `tab_recovered.new_tab_id`；沒給 tab_id 時優先用最近一個非空白分頁；tab_close 找不到則 closed:0 不報錯。
- [x] 桌面版：擴充功能沒連上時 `browser-control-server.js` 自動找 Chrome（後備 Edge，可用 `FA_BROWSER_CONTROL_BROWSER` 指定路徑）啟動並等最多 25 秒，連上後才執行指令（用假瀏覽器腳本驗證：第一次呼叫觸發啟動並成功、第二次不重啟）。連線判定改為看長輪詢是否還開著，瀏覽器被關約 5 秒內判定離線。
- [x] 設定 → 瀏覽器控制多一個「啟用瀏覽器控制」勾選；啟用時 `_runSubAgentTask` 把 15 個 browser_* 工具與一段習慣提示注入**所有**子agent（跟 browser_search 一樣有 enable 就全都認識）。實測系統提示有注入。
- [x] 新工具 `browser_get_page_structure`：標題階層、Markdown正文（含清單/表格/連結）、tables、links、forms，自動略過 nav/footer；提示要求 AI 讀網頁優先用它、回答時結論→條列/表格→來源連結。

## Phase 6：假寫入防護／雙向sync／coding暫存工作區／Python平行運算（2026-09-21）
- [x] **假寫入防護**（gpt-oss:120b 常「說已寫入卻沒呼叫工具」）：記錄每輪是否有「成功的寫入類工具呼叫」（fap_write_file/fs_write_file/apply_git_patch/git_commit/coding_workspace/bash…），最終回覆若宣稱已寫入/儲存/建立檔案、卻沒有任何成功紀錄，就退回請AI真的呼叫工具或如實說沒寫，每輪最多攔2次；根對話（原生＋文字協定）與子agent都有。測試：11個句型（含否定/建議/程式碼區塊不誤判）＋用假LLM端到端驗證。
- [x] **終端機 /mnt 雙向sync**：hydrate 時記錄基準（size/mtime/hash），之後 pull（真實資料夾新增/更新/刪除→沙盒）與 push（只寫有改的檔案、真實檔案被別人改過則視為衝突不覆蓋）；每個指令前重新掃描已授權FAP清單（重新授權馬上出現在/mnt）、碰到已載入的掛載會自動 pull（有節流、大資料夾自動放慢）；相對路徑（cd /mnt; cd label）與cwd都會觸發載入；`sync`＝雙向、`--pull`/`--push`、`--delete` 才會把沙盒刪除傳回真實資料夾。第一次讀取大資料夾會印「正在讀取…／已讀取N個檔案」，指令執行中忽略鍵盤輸入、失敗一律顯示訊息（不再靜默回到原資料夾）。⚠️「cd 要兩次才生效」我在OPFS模擬的資料夾無法重現，上述是針對可能原因（標籤清單過期、hydrate中途失敗仍標記已載入、指令交錯）的修正，需要使用者實測確認。
- [x] **coding 暫存工作區**：不管目標資料夾有沒有git，一律複製到暫存區並 `git init`，patch／測試／還原／commit 都在暫存區做，最後 `coding_workspace deploy`（先 dry_run）逐檔備份、寫入、讀回比對後才轉移回使用者資料夾；用基準清單偵測「使用者原檔在這段期間又被改過」＝衝突不覆蓋，`force` 才強制。桌面版：`os.tmpdir()/floating-assistant/coding/<專案>-<hash>`（`desktop-app/coding-workspace.js`，真的 git）；網頁版：瀏覽器內部儲存 OPFS 的 `fap:ws-<專案>-<hash>`（不是使用者硬碟上的資料夾，提示詞要求AI明確告知）。系統提示新增「步驟0-A」與收尾轉移步驟。
- [x] **Python 平行運算**：Pyodide 移除了 `_multiprocessing`；改用 Web Worker（每個 worker 一份 Pyodide）＋`pyodide.ffi.run_sync`(JSPI) 實作 multiprocessing（Pool.map/starmap/apply_async/map_async/imap/imap_unordered、Process、Queue）與 concurrent.futures.ProcessPoolExecutor（submit/map/as_completed/wait）。規則同真實 spawn：`if __name__ == '__main__'` 底下進入、函式須為最上層具名函式（lambda 會拋 PicklingError）、例外含遠端 traceback 傳回、worker 的 print 會轉回。Value/Array/Manager/Pipe 明確拋 NotImplementedError。shell 的 `python` 指令是同步呼叫無法等 worker → 自動退回單執行緒依序執行並提示。Edge 153 實測：Pool(4)、Process+Queue、futures 平行加速（5個各0.2秒的任務約0.6秒）。限制：需要 JSPI（Chrome/Edge 137+）；每個 worker 首次載入約數秒。

### 本機 proxy 兩種傳輸模式＋瀏覽器控制服務改為用到才啟動（2026-09-21）
- [x] `PROXY_MODE=http|inprocess`（build.ps1 `-ProxyMode` / build.sh `PROXY_MODE` / 環境變數 `FA_PROXY_MODE`，寫入 `build-config.json`）。inprocess 用 `protocol.handle("fa-local")` 直接在主行程跑同一份 `local-proxy.js` 路由，沒有 TCP 監聽埠。用真的 Electron 44（把 dist/win-unpacked 的 asar 換成原始碼資料夾）兩種模式都實測：GET、POST（含中文、Authorization 原樣轉發）、串流回應（分段到達）、OPTIONS、內建 NVIDIA 金鑰路由真的回 200、seed 網址正確。
- [x] 瀏覽器控制的本機服務改成第一次用到才啟動（沒用這功能的人 app 啟動後不再多開埠）；inprocess 模式下瀏覽器控制明確回報不可用。
- 起因：一台裝了 ESET 的電腦把新版判為 Suspicious。這次改動不保證解決，需要對方偵測名稱/路徑才能確認原因。

- [x] 桌面版本機 proxy 補上 `/edge-tts`（中文/粵語/日文/韓文語音）：`desktop-app/edge-tts.js` 移植自 Cloudflare Worker，用主行程內建 WebSocket 連 Microsoft 語音服務。原本本機 proxy 從來沒有這條路由（只有 Worker 有），所以 inprocess/http 兩種模式打過來都是 unknown route。真的 Electron 兩種模式都實測拿到 MP3（`_synthesizeSpeechViaApi` 端到端，長度 2.9 秒）。

- [x] **重複委派防護**（實例：燒錄字幕連續委派十幾次，每次成功、每次都產生30MB影片、始終不給最終回覆）：同一輪對話裡，昂貴/有副作用的工具（delegate_to_subagent、burn_subtitles、transcribe_media…）若「先前已成功且內容相似（delegate 用 domain+task 詞彙相似度≥0.6）」，第二次起不再真的執行，改回傳上次結果並要求模型整理成最終回覆；同輪攔下第2次後主對話改 `tool_choice:none` 強制收尾。根對話（原生＋文字協定）與子agent都有。用假LLM驗證：4次委派（3次同一件事＋1次不同的事）只真的執行2次。

- [x] **編修附件＋輸出下載卡片**：`bash_execute`/`python_execute` 新增 `attachment_files`（{工作目錄檔名: 附件file_id或檔名}，也可給id陣列；二進位檔也行），執行後「新增或內容有變」的檔案存回 persistentStorage 並直接在對話顯示下載卡片（最多5張；原封不動的輸入檔不算產出）。實測：srt 校正、二進位 256 bytes 附件讀取、bash/python 兩邊、找不到附件時明確報錯。
- [x] **分頁讀取**：`fap_read_file`、`parse_uploaded_file`（純文字、zip/tar 項目、PDF 文字）新增 `offset`/`max_chars`/`start_line`/`max_lines`，回傳 `has_more`/`next_offset`/行號資訊；預設頁大小依模型內容窗口自適應（實測預設約3.8萬字元）、盡量停在行尾。精準編修（程式碼、字幕）要分頁讀完整份，不用 summarize。順帶修掉 `real_input_files` 讀 FAP 檔案被靜默截斷在8000字元的 bug。實測 9萬字元檔案分頁串接後與原檔完全相同。

- [x] **`fap_apply_patch`**（改File Access Point檔案「某幾行」，用unified diff）：all-or-nothing、內容對不上時回傳實際內容供重寫、可 `check_only` 乾跑、新增檔案用 `/dev/null`、自動備份、**寫完逐檔讀回比對**（不一致就還原）、回傳 `first_changed_line`/`total_lines_after` 供讀回確認。同一套 `_codingApplyPatch`（順便讓 coding 的 apply_git_patch 也有讀回驗證）。`file_access_points` domain 提示改成「精準編修一律 patch、fap_write_file 只用來新增或整份重寫」。實測：srt 改一行、子資料夾當根、新增檔案、錯誤 context 被擋且不動檔案。

- [x] **`attachment_apply_patch`**（📎純文字附件的 patch 式精準編修）：同一套 `_codingApplyPatch`（all-or-nothing、對不上回傳實際內容、讀回比對），成功後另存成「.已修改」新附件並顯示下載卡片（`in_place:true` 才覆寫原附件），保留 CRLF；二進位附件明確拒絕並指向 `attachment_files`。`file_analysis` domain 提示要求：修改先分頁讀完整份、不可用 summarize、用 patch、讀回確認。實測 srt 修改、乾跑、錯誤 context、in_place、二進位/找不到/缺參數。

- [x] **browser-control extension 跟上 Redmine 移植時實測到的CDP輸入問題**：使用者把同一套瀏覽器控制邏輯移植到 Redmine 時，實測發現4個CDP合成輸入的坑，逐一對照本專案`web/browser-control-extension/background.js`檢查：
  - **背景分頁收不到合成輸入**（確認我方也有這個坑，已修）：`mouse`/`type_text`/`press_key`三個會送合成滑鼠鍵盤事件的指令，改成先呼叫新增的`ensureForeground(tabId)`把分頁切到前景（`chrome.tabs.update({active:true})`+`chrome.windows.update({focused:true})`+等待200ms）再送CDP指令；`screenshot`/`scroll`這類唯讀操作維持背景可用不受影響。
  - **CDP點擊不保證元素拿到焦點**（確認我方也有這個坑，已修）：`mouse`的click/double_click/right_click動作送出後，額外補一次`document.elementFromPoint(x,y).focus()`，避免點擊後緊接著`type_text`打不進去。
  - **keyDown帶text造成打字重複**（防範性跟上，我方原本用單一`keyDown`+`text`、不是造成重複的那種寫法，但改成業界標準的`rawKeyDown`（不帶text）+ 單獨`char`事件（只有它帶text）+`keyUp`，跟Puppeteer同一套模式，避免未來版本的Chrome出現同樣的重複插入）：`pressKey()`。
  - **executeScript參數帶undefined觸發「unserializable value」**：確認我方`scroll`/`mouse`/`type_text`/`get_page_text`/`get_page_structure`/`get_elements`的`args`陣列一律用`null`或已驗證過的值，沒有這個坑，不用改。
  - 同步更新到 `D:\Downloads\videos-嗨投資\web\browser-control-extension\background.js`（main 分支）。`node --check`語法驗證通過；未在真實Chrome擴充功能重新載入實測（背景分頁/點擊focus這兩項行為差異在真實瀏覽器才看得出來，建議使用者下次用到瀏覽器控制時留意）。

## Phase 7：Mermaid互動檢視／圖片縮圖+lightbox／vision model自動路由／瀏覽器分頁讀取／PPTX排版修正／4個影片剪輯指令（2026-09-22）

- [x] **真實Electron實測補做**：複製`dist/win-unpacked`+把`resources/app.asar`換成當下原始碼（跟既有慣例一樣），用`--remote-debugging-port`+Playwright CDP連線，對著使用者真實設定（含真的LLM API金鑰）逐一實測，抓到並修正兩個真實bug：
  - **PPTX圖片等比例縮放實際上會被拉伸**：`sizing:{type:'contain', w, h}`原本以為會等比例縮放，用python-pptx讀出的shape尺寸+LibreOffice渲染成PDF截圖驗證，發現400x900直式測試圖被拉伸成接近方框比例（約1.09:1，應為0.444:1）——這個pptxgenjs選項的實際行為跟文件描述對不上。改成自己算好等比例縮放後的寬高直接當shape本身的w/h（不靠sizing選項），修正後python-pptx讀出的比例精確等於原圖比例（直式0.444、橫式3.0都對）。
  - **interpret_image的refusal過濾對「沒Benchmark過的model」完全沒作用**：真實8個model row（全部`abilityTags`是`null`，沒人按過Benchmark）實測，`openai/gpt-oss-20b`真的回了「抱歉，我目前無法直接查看圖片內容」這種制式拒答，卻因為`row.abilityTags && ...`在abilityTags是null時永遠短路成false，被直接當成「成功」回傳給使用者。改成只有「已確認vision:true」才略過檢查，修正後正確cascade到`openrouter/free`，對一張「綠色圓形+紅色正方形」測試圖給出精確描述（形狀/顏色/位置關係全對）。
  - 其餘功能實測全部正確：mermaid viewer（focus-gated滾輪縮放、scrollbar、拖曳）、圖片lightbox（4個來源縮圖、點擊開啟、切換原始尺寸、Esc關閉）、PPTX文字溢出修正（長段落正確切成多張投影片+fit:shrink安全網）、PPTX表格自動跨頁+repeat表頭（40列表格正確切成兩張、表頭重複）、`extract_clip_range`（真的用Mediabunny trim裁出3秒H.264/AAC MP4）、`extract_clip_range_audio`（正確裁出3秒MP3）、`convert_to_animated_gif`（正確輸出15幀動態GIF）、`convert_video_to_animation`（輸出的YAML通過`_validate2DAnimationYaml`、keyframes結構正確、能在對話裡掛載播放）、瀏覽器讀取分頁（`pageText()`邏輯抽出來用Node跑過offset串接重建/start_line/max_lines/邊界案例等測項全過）、`browser_get_page_structure`的`interpret_images`（mock `_bcCall`模擬真實擴充功能回應，端到端跑通vision解讀並填回`images[].description`）。
  - **未涵蓋**：真實Chrome擴充功能配對流程（`browser_control`整條連線，只mock了`_bcCall`測試桌面端邏輯，沒有真的安裝擴充功能配對）。

- [x] **Mermaid/UML檢視器**（`_mountMermaidViewer`）：新增假scrollbar（依內容真實尺寸與目前pan/zoom狀態換算thumb大小/位置，純視覺指示，不可拖曳）；滾輪縮放改成「focused」（點過這個圖表）才生效，避免使用者滾動對話串時滑鼠經過圖表就被吃掉wheel事件變成縮放，外框顏色+提示文字給明確視覺回饋。
- [x] **圖片統一縮圖+lightbox**：新增`_openImageLightbox`（點縮圖彈出原圖，可再點一次切換縮小置中/原始尺寸，背景/✕/Esc關閉）+`_wireImageLightboxDelegation`（掛在`#ai-chat-body`的delegated click listener，只掛一次）。四個來源都套用`ai-img-thumb`class：AI回覆markdown裡的圖片URL（覆寫marked的image renderer）、tool回傳截圖(`_displayDataUrl`)、`_downloadFile`下載卡片新增image分類+縮圖預覽、使用者📎圖片附件在訊息裡從純文字chip補上縮圖。
- [x] **`interpret_image`：圖片附件/FAP圖片/圖片URL自動挑vision model解讀**：發現`parse_uploaded_file`遇到圖片檔案完全沒有特別處理（會把二進位bytes當文字讀出亂碼），也發現雖然LLM Models清單有`abilityTags.vision`欄位（手動按Benchmark才會測），但完全沒有任何程式碼在模型選擇/委派路由時讀這個欄位。新增`_orderedVisionCandidateRows`（依「已知支援vision」＞「未測過」＞「已知不支援」排序）+`_interpretImageWithVisionModel`（依序嘗試，某個model失敗/回覆像看不懂圖片就換下一個）+`_resolveImageInputForInterpretation`（file_id/image_url/FAP圖片三種來源）。新增`interpret_image`工具，加進`file_analysis` domain（委派過去的subagent自然會用到，達到「委派時以支援讀圖的model為主」的效果，不用改動fallback/round-robin路由機制本身）。`parse_uploaded_file`遇到圖片格式改成明確引導改叫`interpret_image`。
- [x] **瀏覽器讀取支援分頁+圖文穿插**：`web/browser-control-extension/background.js`新增`pageText()`（跟`fap_read_file`/`parse_uploaded_file`同一套分頁契約），`get_page_text`/`get_page_structure`原本是一次讀完+`max_chars`硬截斷，改成頁面腳本先組出完整內容、分頁邏輯統一交給`pageText()`。`get_page_structure`新增`images`欄位（可視、去重、濾小圖示，最多30張）。`browser_get_page_structure`工具新增`interpret_images`（+`interpret_images_count`）：對前幾張圖片呼叫`interpret_image`同一套邏輯補上`images[].description`，預設關閉避免每次讀頁面都多花時間。同步到main分支。
- [x] **PPTX匯出修正**：`_faChunkTextByLength`原本只在段落之間（空行）判斷要不要切投影片，單一段落超過maxChars完全不會被切、超出框高視覺上會溢出；改成單一段落也依maxChars切分（先切單行、單行仍太長再硬切字元），文字框額外加`fit:'shrink'`當安全網。表格投影片把`autoPage:false`改成`autoPage:true`+`autoPageRepeatHeader:true`，交給pptxgenjs自己估算表格渲染高度是否超過投影片剩餘空間、超過就自動加投影片接續＋每頁重複表頭（延續出來的投影片不會套用自訂標題列樣式，是目前pptxgenjs版本的既有限制）。`_faAppendVisualSnapshotSlides`改成async，先用`_faGetImageNaturalSize`讀圖片實際寬高，依長寬比選方框形狀（明顯直式用較窄方框、明顯橫式維持寬方框、接近正方形用居中方框），最終仍靠`sizing:contain`做真正等比例縮放，減少留白浪費。
- [x] **4個影片剪輯功能**（各自都有slash指令+AI工具兩種呼叫方式）：
  - `media-extract-clip-range`/`extract_clip_range`：擷取指定時間範圍的畫面+聲音輸出新MP4，新的`FA_CLIP_EXTRACT_WORKER_SRC`module worker（跟`_burnSubtitles`同一種worker生命週期），靠Mediabunny的`Conversion.init({trim:{start,end}})`裁切，`video:{}`/`audio:{}`盡量走快速stream copy不重新編碼。⚠️`trim`選項是依Mediabunny官方文件的「headline feature」推斷寫法，這次沒有真的跑過真實影片驗證，需要使用者實測確認。
  - `media-extract-clip-range-audio`/`extract_clip_range_audio`：只要聲音，重用`_extractAudio`已經在用的`_decodeAudioBuffer`（Web Audio API），解碼出完整AudioBuffer後在記憶體裡按取樣點切出片段（同取樣率不用重新取樣），跟`_extractAudio`一樣的MP3(預設)/WAV編碼選擇——這個功能不依賴Mediabunny，用的是已經在生產環境跑過的路徑，風險低於clip-range那個。
  - `media-to-animated-gif`/`convert_to_animated_gif`：新增`gifenc`（純JS GIF編碼器，CDN `+esm`動態import，跟mediabunny/kokoro-js同一種載入模式）依賴，畫面來源沿用配音小幫手已經驗證過效能的`Mediabunny CanvasSink`+`_createVideoFrameCursor`循序解碼，逐幀quantize+applyPalette+writeFrame。預設fps=10、最大寬度480px避免產生過大檔案。
  - `media-to-animation`/`convert_video_to_animation`：把影片轉成這個app既有的2D動畫YAML格式（不是發明新格式）——依固定間隔擷取關鍵影格轉JPEG data URL，每格各自建一個`type:'image'` shape疊在同一位置，靠`animation.type:'keyframes'`對opacity做「瞬間切換」模擬逐格flipbook播放（這是2D動畫YAML唯一支援「內容隨時間改變」的動畫類型，keyframes只能內插position/rotation/scale/opacity不能切換src，不需要修改渲染器）。每格都是內嵌base64圖片，`max_frames`預設60上限避免YAML暴增。
  - `extract_clip_range`/`extract_clip_range_audio`加進`video_editing` domain（跟既有配音功能同一個domain，開發者原本的規劃就是「剪輯類工具都歸這裡」）；`convert_to_animated_gif`/`convert_video_to_animation`加進`media_av` domain（跟`render_2d_animation`等既有2D動畫工具同一個domain）。

## Phase 8：終端機AI工具＋檔案傳輸GUI（2026-09-23）

- [x] **終端機AI工具**（讓`/run-terminal`的WASM沙盒（xterm.js+busybox ash）能被AI當成可以設計程式、實際跑起來驗證的環境）：session新增全域唯一`id`（uuid）+可重複的`name`，`terminal_create`/`terminal_list`/`terminal_get_text`/`terminal_run`（單行bash，回傳`{ok,exit_code,stdout,stderr}`，支援`output_file`/`stderr_file`導出到沙盒檔案；獨立偵測「整行就是`cd <path>`」並更新`session.cwd`，因為每次`runtime.run()`都是全新wasm instance、compound `cd x && y`裡的cd效果無法帶出來，這是既有互動式終端機共有的限制，非這次新增）/`terminal_cp_to`/`terminal_cp_from`（來源/目的地三選一：`fap:<名稱>/<路徑>`、附件file_id/檔名、或桌面版真實絕對路徑；`cp_from`不給dst時存成附件+對話下載卡片）六個工具，全部`registerOptional`（domain-gated，只有`coding` domain看得到）。已用真實Electron驗證：id/name定址、exit code、stdout/stderr分離、cwd持久化（含「compound cd不會持久化」的既知限制）、output_file寫入沙盒後讀回一致、cp_to/cp_from三種來源/目的地。
- [x] **終端機檔案傳輸GUI**（終端機widget標題列新增🔀按鈕，開啟FileZilla風格的雙欄對話框）：左邊四個分頁——[當下聊天]（`fileCache`裡的附件/AI產生檔案）、[FAP]（先選已授權的File Access Point再瀏覽子資料夾）、[Working Directory]／[Computer]（後兩個只有桌面版`window.desktopAPI`存在時才顯示，Computer分頁新增`fa:rawfs:listDrives` IPC枚舉磁碟機當瀏覽起點，對應使用者原話「desktop版有working directory，web版沒有」）；右邊固定是這個terminal session的沙盒檔案系統（從`/`開始，完整讀寫刪權限）。複製兩個方向直接重用`terminal_cp_to`/`terminal_cp_from`背後那套`_resolveTerminalCopySource`/`_writeTerminalCopyDestination`（GUI只是把「瀏覽到哪、勾了誰」轉成這些函式已經認得的字串格式，不重新發明複製邏輯），支援多選＋刪除（`window.confirm`二次確認）。**刻意只支援檔案**（資料夾只能點進去瀏覽，不能勾選/複製/刪除）——遞迴複製/刪除資料夾的邊界案例超出這次需求範圍。已用真實Electron驗證：chat↔terminal雙向複製（含用`terminal_run cat`獨立驗證寫入內容正確）、FAP空清單正確顯示、Working Directory正確以workspace資料夾為根瀏覽、Computer正確列出C/D/E/J磁碟機且能點進去瀏覽＋麵包屑正確、終端機端刪除（confirm對話框是桌面版另開的真實BrowserWindow、不是原生JS dialog，測試時需要覆寫`window.confirm`或手動點擊）確認檔案真的被移除。
- [x] `main.js`新增`fa:rawfs:listDrives` IPC（win32逐一嘗試A-Z槽`fs.stat`成功才列入，非win32直接回傳`/`）＋`preload.js`對應`rawfs.listDrives()`。
- 限制：FAP/Working Directory/Computer的刪除分別走`dirHandle.removeEntry`/`window.desktopAPI.rawfs.remove`，這次只驗證了終端機端（沙盒`fsStore.unlinkSync`）的刪除，另外三個是對既有已驗證過的底層API的薄包裝，邏輯上一致但沒有逐一在真實UI點過confirm流程再次確認。

### Phase 8 追加：終端機複製slash指令／桌面版/suggest／FAP失去授權仍顯示（2026-09-23）

- [x] **`/run-terminal-cp-to`／`/run-terminal-cp-from`**：`terminal_cp_to`/`terminal_cp_from`原本只有AI工具（domain-gated），使用者自己想手動搬檔案、不想找AI也不想開🔀GUI時沒有入口。新增兩個slash指令，跟`/run-terminal`同一種`name=xxx`前綴寫法（留空且目前只開一個終端機會自動選用，開多個要求明講避免猜錯目標），直接重用`_resolveTerminalCopySource`/`_writeTerminalCopyDestination`，不重寫邏輯。已用真實Electron驗證：`cp-to`把聊天附件複製進沙盒（用`terminal_run cat`獨立驗證內容）、`cp-from`留空dst正確變成對話下載卡片且不會重複推訊息。
- [x] **桌面版`/suggest`不再是空的**：`chipsProvider`（建議內容來源）原本只有web版`tw_stock_db/index.html`接了（內容是「目前選中的股票」相關業務邏輯，持股診斷/備忘錄），桌面版通用助理沒有這個概念、`bootstrap.js`從沒傳這個選項，導致`/suggest`永遠空清單。新增`buildDesktopSuggestionChips()`（`bootstrap.js`），取這個session已經做出來且驗證過的能力當建議（coding domain修bug、終端機、影音轉檔、瀏覽器控制），有設定Working Directory時第一個建議帶入實際資料夾名稱（跟web版「代入目前選中的股票」同一種精神）。已驗證：正確代入真實資料夾名稱「sunneo」（讀`activeWorkspaceFolder`的closure，換資料夾會自動反映最新值，不用額外接線）。
- [x] **失去授權的FAP不再從終端機/GUI完全隱藏**：原本`/mnt`種子清單（`_ensureTerminalFsStore`／`_refreshTerminalFapLabels`）跟🔀GUI的FAP分頁都只列`permission==='granted'`的FAP，理由是「還沒授權就列出來，一cd進去跳permission dialog太突兀」——但使用者明確要求反過來：失去授權（被瀏覽器重置）的FAP要能被看到/cd到/touch到，一access就跳授權要求，而不是使用者根本不知道曾經有這個FAP存在。改成不篩permission、一律列出；GUI分頁額外標註「🔒需要重新授權」。點進去時`_fapListFiles`/`_hydrateFapMount`內部本來就會呼叫`_checkFapPermission`（權限不是granted就自動跳授權對話框），沒有另外寫觸發邏輯，純粹是拿掉「隱藏」這一步。已驗證：mock一個`permission:'prompt'`的FAP，`ls /mnt`正確同時列出granted跟revoked兩個資料夾、GUI FAP分頁正確顯示`revoked_folder（🔒需要重新授權）`。
- [x] **修正授權對話框被🔀檔案傳輸GUI等新畫面擋住**：`_showFapPermissionDialog`的overlay原本是`z-index:1000020`，比後來陸續加的全螢幕overlay（終端機檔案傳輸GUI、圖片lightbox，都是`2147483000`）低，授權對話框實際上被蓋在下面、使用者看不到也點不到，整個流程卡住。改成`2147483647`（32-bit z-index實務安全上限），確保永遠在最上層。已驗證：開著檔案傳輸GUI時觸發授權對話框，用`elementFromPoint`確認「授權」按鈕所在座標真的回傳按鈕本身（不是被GUI遮住），可以正常點擊。
