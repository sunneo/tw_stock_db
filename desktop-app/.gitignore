node_modules/
dist/
# tw_stock_db客製: 2026-09-17使用者要求——renderer/floating-assistant.js
# 不再是build時從../web/複製過去的gitignored副本，這個desktop-app分支
# 本身現在是floating-assistant.js唯一的canonical來源（直接在這裡編輯、
# commit、push），main分支的web/index.html改成執行時從這個分支的raw URL
# fetch這個檔案（見web/index.html第1687行一帶的既有sql.js載入模式，套用
# 同一招）。main分支不再持有這個檔案的實體副本。
# PyInstaller build artifacts for launcher/launcher.py (build.ps1/build.sh
# regenerate these; launcher.py itself is the tracked source).
launcher/build/
launcher/dist/
launcher/*.spec
launcher/__pycache__/
# tw_stock_db客製: 2026-09-16——每個工作區資料夾（cwd）底下的對話/設定
# 資料（見main.js的fa:workspace:*/bootstrap.js的localStorage代理）。
# 正常情況下cwd=desktop-app本身會被looksLikeAppOwnOrSystemLocation()
# 擋掉、退回使用者家目錄，不該真的寫進這裡；純粹當防呆——萬一開發時用
# 某種cwd邊界情況（例如非標準的啟動方式）繞過了那個判斷，也不會不小心
# 把本機對話紀錄/API金鑰commit進版控。
.floating-assistant/
# tw_stock_db客製: 見build.sh/build.ps1的說明——打包時從建置機器的
# FA_BUILTIN_NVAPI_KEY/FA_BUILTIN_OPENROUTER_KEY環境變數產生，故意不進
# 版控（金鑰值只活在CI/建置者自己的shell環境，不進git history）。也排除
# 編輯器手動編輯這個檔案時可能自動產生的備份檔（例如某些編輯器的.bak/.orig
# 慣例）——內容一樣是真實金鑰，同樣不該進版控。
builtin-secrets.json
builtin-secrets.json.*

# 同理：build.bat是使用者自己本機用來設定FA_BUILTIN_NVAPI_KEY/
# FA_BUILTIN_OPENROUTER_KEY再呼叫build.ps1的捷徑腳本，裡面直接寫死真實金鑰
# 明文，只留在本機、不進版控。
build.bat
